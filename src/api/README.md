# API Layer

Backend service exposing:

- Network data ingestion (nodes, edges, live traffic conditions)
- Route optimization requests (QPSO / baselines)
- Optimized route + convergence metrics as output

Status: not yet implemented.
