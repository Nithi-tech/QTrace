<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only exception to Android's default cleartext-traffic block (targetSdk 28+
  blocks plain http:// by default). Scoped to the loopback/emulator addresses the debug
  backend is actually reached through (see android/README.md - qtrace.apiBaseUrl is
  http://127.0.0.1:8000/ over `adb reverse` on a real device, or http://10.0.2.2:8000/ on
  the emulator) - never a blanket cleartext allowance, and this file is not part of the
  release build (CLAUDE.md #19, #58 - production must use HTTPS).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
